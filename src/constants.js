const AUTH_HEADER_NAME = 'Authorization'
const MAX_PRICE_VALUE = Number.MAX_SAFE_INTEGER

const USER_BASE_INFO_FIELDS = 'email user_name phone_number _id location created_at rating'

module.exports = {
  AUTH_HEADER_NAME,
  MAX_PRICE_VALUE,
  USER_BASE_INFO_FIELDS
}